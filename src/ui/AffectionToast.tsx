/**
 * 호감도 토스트 — 외부 import 슬롯 보존용 진입점.
 *
 * 실제 구현은 `./affection/AffectionToastStack.tsx` (UI-SPEC §10).
 * 풍성 온도계 + 미니 도트 + 묶음 5개 동시 + spring 채움 + 사운드 등 모든 동작을 그쪽이 담당.
 */

export { AffectionToastStack as AffectionToast } from './affection/AffectionToastStack';
